import { StyleSheet } from "react-native"
const style = StyleSheet.create({
    container: {
        margin: 32,

    },
    subtitle: {
        fontSize: 20,
        color: 'black',
        marginTop: 40,
        marginBottom: 34
    }
})

export default style;